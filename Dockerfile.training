# Dockerfile.training
FROM python:3.11-slim

WORKDIR /app

# O serviço de treinamento só precisa do FastAPI e Uvicorn por agora.
# Não precisa de toda a stack de IA, o que o torna mais leve.
RUN pip install --no-cache-dir fastapi "uvicorn[standard]"

COPY training_service.py .
COPY feedback.db .

EXPOSE 8002

CMD ["uvicorn", "training_service:app", "--host", "0.0.0.0", "--port", "8002"]